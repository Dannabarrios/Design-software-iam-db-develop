# Uso del tablero IAM DB

## Flujo de estado

`Backlog → Refinement → Ready → In Progress → In Review → Done`

## Flujo de ambiente

`Sin promover → Dev → QA → Main`

Cada HU utiliza ramas hijas independientes para `develop`, `qa` y `main`. Nunca se mezclan directamente las ramas padre entre sí.

